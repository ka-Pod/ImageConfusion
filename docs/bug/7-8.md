图片混淆完全无法使用，使用skills，看看git的历史，之前v1.4.7还可以使用的
I now have a complete diagnosis. Here's my analysis:
Root Cause: File Upload Completely Broken
The dual-input architecture creates a dead path:
Problem 1: ControlBar.vue onFilesChanged (lines 42-50)
function onFilesChanged(event: Event) {
  const input = event.target as HTMLInputElement
  if (input.files && input.files.length > 0) {
    if (input.files.length === 1) {
      emit('select-files')   // ❌ No data passed!
      // The parent handles reading the files  ← WRONG: parent never sees files
    }
  }
}
Three critical bugs in this one function:
1. Emits event without the File objects — parent receives nothing
2. Only handles single file (length === 1) — selecting multiple files does nothing
3. Same handler for ALL 3 inputs — "选择图片", "选择文件夹", "上传ZIP" all need different handling but use the same broken function
Problem 2: ConfusePage.vue has orphaned hidden inputs (lines 278-299)
ConfusePage has its own hidden file inputs with correct change handlers (onFileSelected, onDirSelected, onZipSelected), but:
- These inputs are never triggered — no code calls fileInput.value?.click()
- ControlBar's buttons trigger ControlBar's own local file input refs, not ConfusePage's
Why v1.4.7 worked
The old src/ui.ts used a single monolithic HTML page with inline JS — file inputs were directly wired to their handlers. The Vue migration split these into two components, introducing a gap where ControlBar has the buttons but doesn't pass data, and ConfusePage has the handlers but no triggers.
Flow diagram
User clicks "选择图片" in ControlBar
  → ControlBar's local fileInput.click()
  → ControlBar's onFilesChanged() fires
  → emits 'select-files' WITH NO DATA
  → ConfusePage has NO @select-files handler
  → NOTHING HAPPENS ❌

The only working path: drag-and-drop via DropZone
  → DropZone emits files
  → ConfusePage.onDropFiles() handles it ✅
Plan
1. Fix ControlBar.vue: Replace broken onFilesChanged with separate handlers that emit the actual File[] 
2. Fix ConfusePage.vue: Add handlers for ControlBar file events, remove orphaned hidden inputs
3. Verify with bun test + pnpm lint + manual testing
Ready to proceed?
# Questions
Ready to fix the file upload bug?
Review more
+ Thought: 1.2s
→Read src\client\components\confuse\ControlBar.vue 
→Read src\client\pages\ConfusePage.vue [offset=1, limit=50]
→Read src\client\composables\useConfuse.ts 
+ Thought: 6.5s
→Read src\client\components\common\DropZone.vue 
+ Thought: 21.8s
Here's the detailed step-by-step plan:
Detailed Fix Plan
Root Cause Recap
ControlBar has the buttons + file inputs but its onFilesChanged handler is broken (emits no data, only handles single file, same handler for all 3 input types). ConfusePage has handlers that CAN process files, but they're wired to orphaned hidden inputs that are never triggered. The gap between the two components means clicking any upload button does nothing.
Changes
1. ControlBar.vue — Fix file handling
3 separate change handlers instead of the broken single onFilesChanged:
Input	Current Handler	New Handler	Emits
选择图片 (file)	onFilesChanged (broken)	onImageFilesChanged	files-selected with File[]
选择文件夹 (dir)	onFilesChanged (broken)	onDirFilesChanged	files-selected with File[]
上传ZIP (zip)	onFilesChanged (broken)	onZipFileChanged	zip-selected with single File
Emits change from data-less void to typed with payload:
// Before (broken):
(e: 'select-files'): void
(e: 'select-folder'): void
(e: 'select-zip'): void

// After:
(e: 'files-selected', files: File[]): void
(e: 'zip-selected', file: File): void
2. ConfusePage.vue — Wire events + clean up orphans
Add handlers to ControlBar usage (line ~257):
@files-selected="onDropFiles"     <!-- reuse existing handler -->
@zip-selected="onDropZip"         <!-- reuse existing handler -->
Remove orphaned elements that are never triggered:
- Lines 28-31: fileInput, dirInput, zipInput refs (nobody calls .click() on them)
- Lines 177-199: onFileSelected, onDirSelected, onZipSelected methods (handlers for orphaned inputs)
- Lines 275-299: 3 hidden <input> elements (orphaned, never clicked)
Wait — onZipSelected is also referenced from the orphaned ZIP input's @change. But onDropZip already handles the same logic. So the orphaned ZIP input + its handler are both dead code.
Vite HMR note: When we remove the orphaned inputs, we also need to make sure the ZIP button path still works (it will, because we wire @zip-selected="onDropZip" to ControlBar).
What This Fix Enables
1. Click "选择图片" → pick single image → preview shows → encrypt/decrypt/restore/download work
2. Click "选择图片" → pick multiple → batch mode → encrypt all → save as comic
3. Click "选择文件夹" → batch mode with all images
4. Click "上传ZIP" → decrypt ZIP → batch preview
5. Drag-and-drop (already works, unchanged)
What This Does NOT Touch
- useConfuse.ts — file loading logic unchanged (already correct)
- API routes — already verified working
- Gallery — separate system, already verified working
- batch.ts, confuse.ts, gilbert.ts — core algorithms unchanged
Verification After Fix
1. pnpm lint — must be zero errors
2. bun test — all 110 tests must pass
3. Manual test: open http://localhost:5173/confuse, click "选择图片", upload, encrypt, decrypt, restore
4. Manual test: batch mode with multiple files
Does this plan look correct? Shall I implement it?
同时，漫画画廊点击解密工作不正常，解密的图片是混淆过的。
+ Thought: 1m 53s
Good catch. Let me investigate the gallery decrypt issue in parallel. Let me trace the full create → decrypt flow for the gallery path.
→Read src\batch.ts [limit=60]
→Read src\confuse.ts 
+ Thought: 622ms
→Read src\batch.ts [limit=50, offset=140]
+ Thought: 33.9s
Now let me look at the gallery decrypt issue specifically:
→Read src\server\gallery-storage.ts [limit=50, offset=101]
+ Thought: 16.6s
Here's my combined analysis of both issues:
Two Bugs Found
Bug 1: File Upload (Root cause confirmed)
ControlBar.vue onFilesChanged — emits events without the File objects. The parent never receives data. Only drag-and-drop works.
Bug 2: Gallery Decrypt — Needs investigation
The decrypt code logic looks correct on paper — encryptPixels with PNG (lossless) → store → decryptPixels should produce correct output. Tests confirm the round-trip works. But the user reports the result is still obfuscated.
There are a few potential causes:
A) The decryptComic function at gallery-storage.ts:113-114 calls sharp twice on the same buffer — once for raw pixels, once for metadata. This should be fine, but let me verify with a concrete test.
B) Old test data in storage/ from before the PNG fix — if any comics were created with the old JPEG-based encrypt, decrypt would fail because JPEG artifacts corrupted the encrypted pixel data.
C) The save-from-batch JSON body path passes zipBuffer as base64 string → decodes as Buffer → passes to extractZipBuffer → filters to image files → converts to PNG WITHOUT encrypting. If the extracted images are NOT already encrypted, they'd be stored raw and later decryptPixels would scramble them instead.
Normal flow:    encrypt(pixels) → store → decrypt(pixels) → original ✅
Bad flow (#C):  raw pixels     → store → decrypt(pixels) → SCRAMBLED ❌
D) Frontend issue — the decrypt works but the reader doesn't display correctly.
Plan
Part 1: Fix file upload (high confidence)
1. Fix ControlBar.vue onFilesChanged → 3 separate handlers that emit File[] data
2. Remove orphaned hidden inputs + unused handlers from ConfusePage.vue
3. Wire @files-selected and @zip-selected in ConfusePage's ControlBar usage
Part 2: Investigate gallery decrypt
1. Write an E2E test: create comic with known pixel values → decrypt → verify output matches
2. If test passes → issue is frontend or old data → investigate ReaderPage/Cleanup
3. If test fails → add diagnostic logging to decryptComic to trace where pixel corruption occurs
4. Check the save-from-batch JSON path for the raw-pixels-getting-stored issue (cause C above)
Proceed with the plan?
▣  Plan · DeepSeek V4 Pro · 3m 13s
写好plan，根据AGENTS.md和第一性原理和对抗性审查，修复上面的bug。
